---
title: "AWS CLI cheat sheet"
description: "AWS CLI commands grouped by how often you'll need them. Only commands verified against AWS documentation are listed; it grows with each level."
domain: aws
pageType: cheatsheet
status: draft
sources:
  - title: "Installing or updating to the latest version of the AWS CLI"
    url: "https://docs.aws.amazon.com/cli/latest/userguide/getting-started-install.html"
    publisher: "AWS CLI User Guide"
  - title: "Configuring IAM Identity Center authentication with the AWS CLI"
    url: "https://docs.aws.amazon.com/cli/latest/userguide/cli-configure-sso.html"
    publisher: "AWS CLI User Guide"
  - title: "Sign up for AWS (advanced) — aws login"
    url: "https://docs.aws.amazon.com/accounts/latest/reference/getting-started.html"
    publisher: "AWS Account Management Reference Guide"
tags: [aws, cli, cheat-sheet]
sidebar:
  order: 90
---

import { Tabs, TabItem } from '@astrojs/starlight/components';

Level 0 covers installing, signing in, and identity. Each later level adds the commands it teaches (S3, EC2, IAM, and so on) once they're checked against AWS's docs.

## Common

| Command | What it does |
| --- | --- |
| `aws --version` | Shows the installed CLI version. Confirm it starts with `aws-cli/2.` |
| `aws sso login --profile <name>` | Signs in through your browser and caches an SSO session for that profile |
| `aws sts get-caller-identity --profile <name>` | Shows which account and identity your commands are using. Run it first whenever you get "access denied". |
| `aws <service> <command> --profile <name>` | Runs any command as a given profile |
| `aws sso logout` | Deletes cached SSO credentials for all profiles |

## Uncommon

| Command | What it does | Notes |
| --- | --- | --- |
| `aws configure sso` | Wizard that creates an `sso-session` and a profile in `~/.aws/config` | Enter a session name; blank means legacy, non-refreshing mode |
| `aws configure sso-session` | Creates or updates only an `sso-session` section | Not compatible with legacy Identity Center setup |
| `aws sso login --sso-session <name>` | Logs in one session shared by several profiles | Not available for legacy setup |
| `aws sso login --profile <name> --use-device-code` | Signs in with a URL and code you can approve on another device | Default since 2.22.0 is browser-based PKCE, which must be on the same machine |
| `aws update` | Updates the CLI in place | For script or official-installer installs; run elevated for all-users installs |
| `aws login` | Newer programmatic sign-in; rotates credentials every 15 minutes, sessions up to 12 hours | Requires CLI 2.32.0+ |

## Rare

:::caution
Rare commands are the easiest to get wrong. Each links to its official documentation; run them only after reading the linked page.
:::

<Tabs syncKey="os">
<TabItem label="Windows">

| Command | What it does | Source |
| --- | --- | --- |
| `irm https://awscli.amazonaws.com/v2/install.ps1 \| iex` | Installs the CLI for the current user | [AWS install guide](https://docs.aws.amazon.com/cli/latest/userguide/getting-started-install.html) |
| `irm 'https://awscli.amazonaws.com/v2/install.ps1' -OutFile install.ps1; try { .\install.ps1 -System } finally { Remove-Item install.ps1 }` | Installs for all users (Administrator PowerShell) | [AWS install guide](https://docs.aws.amazon.com/cli/latest/userguide/getting-started-install.html) |
| `msiexec.exe /i https://awscli.amazonaws.com/AWSCLIV2.msi /qn` | Silent all-users MSI install | [AWS install guide](https://docs.aws.amazon.com/cli/latest/userguide/getting-started-install.html) |
| `msiexec.exe /i https://awscli.amazonaws.com/AWSCLIV2-User.msi /qn` | Silent current-user MSI install, no admin rights | [AWS install guide](https://docs.aws.amazon.com/cli/latest/userguide/getting-started-install.html) |

</TabItem>
<TabItem label="macOS">

| Command | What it does | Source |
| --- | --- | --- |
| `curl -fsSL https://awscli.amazonaws.com/v2/install.sh \| bash` | Installs the CLI for the current user | [AWS install guide](https://docs.aws.amazon.com/cli/latest/userguide/getting-started-install.html) |
| `curl -fsSL https://awscli.amazonaws.com/v2/install.sh \| sudo bash -s -- --system` | Installs for all users | [AWS install guide](https://docs.aws.amazon.com/cli/latest/userguide/getting-started-install.html) |
| `curl -fsSL https://awscli.amazonaws.com/v2/install.sh \| bash -s -- --help` | Lists the install script's options | [AWS install guide](https://docs.aws.amazon.com/cli/latest/userguide/getting-started-install.html) |
| `sudo installer -pkg AWSCLIV2.pkg -target /` | Installs a downloaded PKG for all users | [AWS install guide](https://docs.aws.amazon.com/cli/latest/userguide/getting-started-install.html) |

</TabItem>
<TabItem label="Linux">

| Command | What it does | Source |
| --- | --- | --- |
| `curl -fsSL https://awscli.amazonaws.com/v2/install.sh \| bash` | Installs the CLI for the current user | [AWS install guide](https://docs.aws.amazon.com/cli/latest/userguide/getting-started-install.html) |
| `gpg --verify awscliv2.sig awscliv2.zip` | Verifies a manually downloaded installer's signature (after importing AWS's public key) | [AWS install guide](https://docs.aws.amazon.com/cli/latest/userguide/getting-started-install.html) |
| `sudo ./aws/install` | Runs the unzipped command line installer | [AWS install guide](https://docs.aws.amazon.com/cli/latest/userguide/getting-started-install.html) |
| `sudo yum remove awscli` | Removes Amazon Linux's pre-installed old CLI before installing v2 | [AWS install guide](https://docs.aws.amazon.com/cli/latest/userguide/getting-started-install.html) |

</TabItem>
</Tabs>

## Files and locations

| Path | Holds |
| --- | --- |
| `~/.aws/config` (Windows: `%USERPROFILE%\.aws\config`) | Profiles and `sso-session` sections |
| `~/.aws/sso/cache` | Cached SSO tokens |
